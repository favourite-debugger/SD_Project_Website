<%= form_with(model: block) do |form| %>
  <% if block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(block.errors.count, "error") %> prohibited this block from being saved:</h2>

      <ul>
        <% block.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "BLock Start Date: "%>
    <%= form.date_select :BlockStartDate %>
  </div>

  <div class="field">
    <%= form.label "BLock End Date:  "%>
    <%= form.date_select :BlockEndDate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
