
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

  <div class="container-fluid">
    <%= link_to 'Student Placement System', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">


        <li class="nav-item">
          <% if controller_name == "home"%>
          <!-- If we're at the Dashboard view, set this nav link to active -->
            <%= link_to 'Home', root_path, class:"nav-link active" %>
          <!-- If we're at another view, set this to normal nav link -->
          <% else %>
            <%= link_to 'Home', root_path, class:"nav-link" %>
          <% end %>
        </li>


        <li class="nav-item">
          <!-- If we're at the Manage Users view, set this nav link to active -->
          <% if controller_name == "users"%>
            <%= link_to 'Users', users_path, class:"nav-link active" %>
          <!-- If we're at another view, set this to normal nav link -->
          <% else %>
            <%= link_to 'Users', users_path, class:"nav-link" %>
          <% end %>
        </li>

        <li class="nav-item">
          <!-- If we're at the Manage Users view, set this nav link to active -->
          <% if controller_name == "hospitals"%>
            <%= link_to 'Hospitals', hospitals_path, class:"nav-link active" %>
          <!-- If we're at another view, set this to normal nav link -->
          <% else %>
            <%= link_to 'Hospitals', hospitals_path, class:"nav-link" %>
          <% end %>
        </li>


      </ul>
      

      <!-- Add search forms for OWN model -->
      <!-- unless statement takes out search bar when editing,creating,etc-->
      <% unless url_for != users_path %>
      <!--Makes the seach field neater however cant find a way to pull search field to  right of navbar -->
        <form class="form-inline my-2 my-lg-0">
          <%=render "users/filters"%>|
          <%=link_to "Clear search",users_path,class: "btn btn-outline-info"%>
        </form>
      <% end %>
       
      <% unless url_for != hospitals_path %>
        <form class="form-inline my-2 my-lg-0">
          <%=render "hospitals/filters"%> |
          <%=link_to "Clear Search",hospitals_path,class: "btn btn-outline-info"%> 
        </form>
      <% end %>
      <!--ADD Below check users controller and 
       -filters.html.erb for eg -->

  
    </div>
  </div>

</nav>
